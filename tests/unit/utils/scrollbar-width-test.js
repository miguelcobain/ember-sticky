import scrollbarWidth from 'dummy/utils/scrollbar-width';
import { module, test } from 'qunit';

module('Unit | Utility | scrollbar width');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = scrollbarWidth();
  assert.ok(result);
});
