import isInViewport from 'dummy/utils/is-in-viewport';
import { module, test } from 'qunit';

module('Unit | Utility | is in viewport');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = isInViewport();
  assert.ok(result);
});
