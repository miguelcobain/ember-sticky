import canUseRaf from 'dummy/utils/can-use-raf';
import { module, test } from 'qunit';

module('Unit | Utility | can use raf');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = canUseRaf();
  assert.ok(result);
});
